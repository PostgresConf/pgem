<%= render 'refinery/admin/search_header', :url => refinery.meetups_admin_meetups_path %>
<div class='pagination_container'>
  <% if @meetups.any? %>
    <%= render 'meetups' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
