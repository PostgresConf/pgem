<% content_for :body do %>
  <ul id="meetups">
    <% @meetups.each do |meetup| %>
      <li>
        <%= link_to meetup.title, refinery.meetups_meetup_path(meetup) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
