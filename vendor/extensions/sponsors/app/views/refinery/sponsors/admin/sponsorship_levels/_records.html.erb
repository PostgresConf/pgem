<%= render 'refinery/admin/search_header', :url => refinery.sponsors_admin_sponsorship_levels_path %>
<div class='pagination_container'>
  <% if @sponsorship_levels.any? %>
    <%= render 'sponsorship_levels' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
